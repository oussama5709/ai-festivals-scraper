{
  "title": "AI Festivals & Conferences Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchRegions": {
      "title": "🌍 المناطق الجغرافية | Search Regions",
      "description": "اختر المناطق التي تريد البحث فيها | Select regions to search in",
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "worldwide",
          "middle-east",
          "africa",
          "europe",
          "asia",
          "americas"
        ]
      },
      "default": [
        "worldwide",
        "middle-east",
        "africa",
        "europe",
        "asia",
        "americas"
      ],
      "editor": "checkbox"
    },
    "upcomingOnly": {
      "title": "📅 الأحداث القادمة فقط | Only Upcoming Events",
      "description": "اجمع فقط الأحداث التي لم تحدث بعد | Collect only events that haven't happened yet",
      "type": "boolean",
      "default": true
    },
    "minDate": {
      "title": "📆 التاريخ الأدنى | Minimum Date",
      "description": "لا تجمع أحداث قبل هذا التاريخ | Don't collect events before this date (YYYY-MM-DD)",
      "type": "string",
      "format": "date",
      "default": "2025-03-03"
    },
    "maxResults": {
      "title": "📊 أقصى نتائج | Maximum Results",
      "description": "الحد الأقصى لعدد الأحداث المطلوب جمعها | Maximum number of events to collect",
      "type": "integer",
      "minimum": 10,
      "maximum": 1000,
      "default": 100
    },
    "includeEventTypes": {
      "title": "🎯 أنواع الأحداث | Event Types to Include",
      "description": "اختر أنواع الأحداث المراد تضمينها | Select which types of events to include",
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "conference",
          "workshop",
          "meetup",
          "webinar",
          "summit",
          "hackathon",
          "course"
        ]
      },
      "default": [
        "conference",
        "workshop",
        "meetup",
        "webinar"
      ],
      "editor": "checkbox"
    },
    "dataSources": {
      "title": "🔗 مصادر البيانات | Data Sources",
      "description": "اختر المصادر التي تريد جمع البيانات منها | Select data sources to scrape",
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "eventbrite",
          "meetup",
          "linkedin",
          "official-websites",
          "conference-aggregators"
        ]
      },
      "default": [
        "eventbrite",
        "meetup",
        "official-websites"
      ],
      "editor": "checkbox"
    },
    "language": {
      "title": "🌐 اللغة | Language",
      "description": "لغة النتائج والوصف | Output language",
      "type": "string",
      "enum": [
        "ar",
        "en"
      ],
      "default": "ar"
    },
    "outputFormat": {
      "title": "📄 صيغة الإخراج | Output Format",
      "description": "صيغة الإخراج النهائي | Format of final output",
      "type": "string",
      "enum": [
        "json",
        "csv",
        "xlsx"
      ],
      "default": "json"
    },
    "proxyConfiguration": {
      "title": "🔐 إعدادات البروكسي | Proxy Configuration",
      "type": "object",
      "description": "استخدم بروكسي لتجنب الحظر | Use proxy to avoid getting blocked",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": [
    "searchRegions"
  ]
}
